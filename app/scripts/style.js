// Fixes the padding for the .current selector of the 'li' item
// $('.current i').css('padding-left','5.2em');

var position = $('.tab-content').position();
$('.list-group').first().css('margin-top', position.top);